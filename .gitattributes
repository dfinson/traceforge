# Model weight binaries are tracked via Git LFS (kept out of the git object store).
# See packages/traceforge-title-model (span titler) and src/traceforge/phase (phase model).
*.onnx filter=lfs diff=lfs merge=lfs -text
*.safetensors filter=lfs diff=lfs merge=lfs -text
