name: Test

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
      - uses: astral-sh/setup-uv@v7
      - run: uv sync --group dev
      - run: uv run pytest --tb=short -q

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - run: pip install hatchling build
      - run: python -m build
      # tracemill now depends on tracemill-title-model, which is not on PyPI until
      # its own release tag is pushed. Build it into dist/ and resolve it locally
      # via --find-links so the base-wheel install works pre-publish (litellm,
      # onnxruntime, etc. still come from PyPI).
      - run: python -m build packages/tracemill-title-model --outdir dist
      - run: pip install --find-links dist tracemill
      - run: python -c "import tracemill; print(tracemill.__name__)"
